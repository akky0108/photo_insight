name: Merge Envs and Run Tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  merge-env:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyyaml

    - name: Merge environments
      run: |
        python photo_eval_env_manager/merge_envs.py \
          --base environment_base.yml \
          --final environment_combined.yml \
          --requirements requirements.txt \
          --ci environment_ci.yml \
          --exclude exclude_ci.txt

    - name: Upload combined env for next job
      uses: actions/upload-artifact@v4
      with:
        name: env-files
        path: |
          environment_ci.yml
          requirements.txt

  test:
    needs: merge-env
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Download merged environment files
      uses: actions/download-artifact@v4
      with:
        name: env-files

    - name: Set up Miniconda
      uses: conda-incubator/setup-miniconda@v2
      with:
        auto-activate-base: false
        environment-file: environment_ci.yml
        activate-environment: photo_eval_env
        python-version: 3.10

    - name: Create conda env
      run: |
        conda env remove -n test-env || true
        conda env create -n test-env -f environment_ci.yml

    - name: Activate env and install test dependencies
      shell: bash -l {0}
      run: |
        conda activate test-env
        conda install -y pytest pytest-mock

    - name: Run tests
      shell: bash -l {0}
      run: |
        conda activate test-env
        pytest --maxfail=1 --disable-warnings -q
